import RegisterModel from "@/components/Register";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <RegisterModel />
    </div>
  );
};

export default page;
